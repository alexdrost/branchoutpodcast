{
  "name": "branch-out-podcast",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "node validate.mjs && astro build && pagefind --site dist",
    "preview": "astro preview",
    "validate": "node validate.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.11",
    "@astrojs/sitemap": "^3.3.0",
    "astro": "^5.6.1",
    "js-yaml": "^4.1.0",
    "pagefind": "^1.3.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "wrangler": "^4.0.0"
  }
}